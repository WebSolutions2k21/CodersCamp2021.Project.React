import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
    all: {
      fontFamily: '"Ubuntu", sans-serif',
    },
    title: {
      fontSize: '26px',
      color: "#16BAC6",
      pb: "14px",
    },
    bold: {
      fontWeight: "900",
    },
});